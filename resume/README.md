# Resume

The canonical resume source is `yuchen-liu-resume.tex`.

Compile with:

```bash
pdflatex yuchen-liu-resume.tex
```

The local machine used to scaffold this repository did not have a TeX compiler installed, so the PDF in this directory may be regenerated in a TeX environment before submission.
