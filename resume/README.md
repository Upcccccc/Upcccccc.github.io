# Resume

The application-ready PDF is `yuchen-liu-resume.pdf`. The editable LaTeX source is `yuchen-liu-resume.tex`.

Compile with:

```bash
pdflatex yuchen-liu-resume.tex
```

The checked-in PDF was generated with:

```bash
python build_resume_pdf.py
```
